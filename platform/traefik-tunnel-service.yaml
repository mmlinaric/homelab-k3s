apiVersion: v1
kind: Service
metadata:
  name: traefik-tunnel
  namespace: traefik
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: traefik
  ports:
    - name: tunnel
      port: 8443
      targetPort: 9443
      protocol: TCP

